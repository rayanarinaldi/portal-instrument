/**
 * Backend CORS reminder.
 *
 * If your api-server uses Express, add:
 *
 * import cors from "cors";
 * app.use(cors({
 *   origin: [
 *     "http://localhost:5173",
 *     "https://your-frontend.vercel.app"
 *   ],
 *   credentials: true
 * }));
 *
 * If it already uses CORS, only update the allowed origin list.
 */
